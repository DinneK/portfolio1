import React, { useState } from 'react'

interface Props {
    image: string;
    title: string;
    text: string;
}

const ProjectCard = ({ image, title, text}: Props) => {
  
  return (
    <div>
      Place Holders
    </div>
  )
}

export default ProjectCard